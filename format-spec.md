# Format Spec · 法律文书 Markdown → Word 排版规范

> 本文件是 `scripts/md2docx_legal.py` 与 `scripts/verify_docx.py` 的参数源。
> 所有诉讼文书转 Word 时严格按本规范执行；用户可编辑本文件以适配本地法域要求。
>
> 默认值来源：中国最高人民法院诉讼文书样式模版 v2020（6 份 `.docx` 实测提取，5/6 一致为准）

## 元信息

| 参数 | 值 |
|------|------|
| 规范名 | 中国大陆法院诉讼文书通用格式 |
| 来源 | 最高人民法院诉讼文书样式模版 v2020（6 份实测均值） |
| 规范版本 | 1.0.0 |
| 适用范围 | 民商事一审 / 二审 / 再审 / 仲裁一切法院提交文书 |
| 最后更新 | 由冷启动访谈 或用户手动编辑 |

---

## 一、页面设置

| 参数 | 值 | OOXML |
|------|------|-------|
| 纸张 | A4 (210mm × 297mm) | w:w="11906" w:h="16838" |
| 上边距 | 2.54cm | top="1440" |
| 下边距 | 2.54cm | bottom="1440" |
| 左边距 | 3.17cm | left="1800" |
| 右边距 | 3.17cm | right="1800" |
| 页眉距 | ~1.5cm | header="851" |
| 页脚距 | ~1.75cm | footer="992" |
| 文档网格 | lines/312 | linePitch="312" |

---

## 二、文书标题

| 参数 | 值 |
|------|------|
| 字体 | 宋体（ascii / eastAsia / hAnsi 均设宋体） |
| 字号 | 二号 = 22pt = sz=44 |
| 加粗 | 否 |
| 对齐 | 居中 (center) |
| 行距 | 不单独设置（默认） |

---

## 三、正文

| 参数 | 值 |
|------|------|
| 字体 | 宋体 |
| 字号 | 四号 = 14pt = sz=28 |
| 行距 | 固定值 25 磅（line=500, lineRule=exact） |
| 首行缩进 | 2 字符（firstLineChars=200, firstLine=560 twips） |
| 段前 / 段后 | 0 |

---

## 四、段落标题

（例如"请求事项："、"事实和理由："、"诉讼请求："）

| 参数 | 值 |
|------|------|
| 字体 | 宋体 |
| 字号 | 四号 (sz=28) |
| 加粗 | 否 |
| 对齐 | 左对齐 + 首行缩进 2 字符 |
| 格式 | 冒号结尾，与正文同段 |

---

## 五、落款区域

### "此致"

| 参数 | 值 |
|------|------|
| 字体 | 宋体四号 |
| 对齐 | 左对齐 + 首行缩进 2 字符 |

### "XX 人民法院 / XX 仲裁委员会"

| 参数 | 值 |
|------|------|
| 字体 | 宋体四号 |
| 对齐 | 左对齐（无缩进，顶格） |

### 签名 / 盖章行

| 参数 | 值 |
|------|------|
| 字体 | 宋体四号 |
| 对齐 | 右对齐 |

### 日期行

| 参数 | 值 |
|------|------|
| 字体 | 宋体四号 |
| 对齐 | 右对齐 |
| 兼容格式 | 2026年 7 月 15 日 或 2026年 月 日（月/日留白供手写） |

---

## 六、附件

| 参数 | 值 |
|------|------|
| "附：" | 宋体四号，左对齐 + 首行缩进 2 字符 |
| 附件列表 | 宋体四号，左对齐 + 首行缩进 2 字符 |

---

## 七、字号换算速查

| 中文字号 | pt | OOXML sz |
|----------|------|----------|
| 二号 | 22pt | 44 |
| 三号 | 16pt | 32 |
| 四号 | 14pt | 28 |
| 小四 | 12pt | 24 |
| 五号 | 10.5pt | 21 |

---

## 八、禁止事项（硬约束）

1. 禁止使用 Arial / Times New Roman 等英文字体作为中文文书主体字体
2. 禁止标题加粗（法院文书标题惯例不加粗）
3. 禁止段落标题加粗或居中（应左对齐 + 首行缩进 + 冒号结尾）
4. 禁止"此致"无缩进或居中（应首行缩进 2 字符）
5. 禁止法院名称右对齐（应左对齐顶格无缩进）
6. 禁止签名行 / 日期行左对齐（应右对齐）
7. 禁止附件区域右对齐（应左对齐 + 首行缩进）
8. 禁止使用 Unicode 标点符号（如 • ●）作为列表标记
9. 禁止正文使用小四或五号字体（必须四号）
10. 禁止行距使用单倍 / 1.5 倍等倍数行距（应使用固定值 25 磅）

---

## 九、内容规则参数（可覆写）

以下参数由排版闸门在 Step 1（内容规范检查）阶段读取，可按需求覆写：

### 9.1 屏蔽的书籍 / 作者名单（R2 规则参数）

排版闸门 R2 规则会检测正文是否引用了本清单中的作者或书籍。默认为空，用户按需填入：

```yaml
blocked_authors: []
blocked_books: []
```

填写示例：

```yaml
blocked_authors: ["某某教授", "某某学者"]
blocked_books: ["《某某原理与判例》"]
```

### 9.2 来源标记格式白名单（R5 规则参数）

排版闸门 R5 规则会检测正文的来源标记格式，仅允许下列格式：

```yaml
citation_tags:
  - "[参考：litigation-knowledge-base]"
  - "[参考：联网检索]"
  - "[参考：legal-verification]"
```

用户可增删。默认已足够普适，不建议大幅修改。

### 9.3 校验阈值（自动修复门槛）

Word 后验校验（V1-V12）失败后的自动修复策略：

```yaml
verify_thresholds:
  auto_fix_max: 2       # 偏差≤N项时自动修复，否则退回
  reject_min: 3         # 偏差≥N项时直接退回，不自动修复
```

---

## 十、扩展点

如需为特定法域 / 法院提交样式定制参数，建议：

1. 复制本文件为 `format-spec.<court-name>.md`
2. 修改对应字段
3. 在 `profile.md` 的 `format_spec_path` 字段指向该文件
4. `md2docx_legal.py` 会优先读取指向的文件

支持覆写字段：所有上述参数均可覆写；不覆写的字段沿用本默认值。
