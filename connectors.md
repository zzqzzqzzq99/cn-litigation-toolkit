# 连接器依赖说明 (CONNECTORS)

> 本套件设计为**无连接器即可完整使用**。连接器为可选增强，接入后能力更强、自动化程度更高。
> 本套件通用于任何支持 MCP 协议的 Agent 运行时（Cursor、Claude Code、Gemini CLI、OpenCode 等）。

---

## 设计原则

1. **无连接器可用**：所有技能在无任何 MCP 连接时仍可正常工作，仅能力降级
2. **优雅降级**：每个连接器不可用时，技能自动切换降级方案并标注
3. **按需连接**：用户可根据实际权限逐步接入连接器
4. **平台无关 + 多供应商灾备**：法律检索、工商信息、文档解析、协作平台、知识库五类外部能力均**面向"能力"而非某一家工具名**（`LAW.*` / `BIZ.*` / `PARSE.*` / `COLLAB.*` / `KB.*`）。同类可配多家供应商，用户用哪家都能跑；配多家则自动灾备。skill 内部只声明所需能力，运行时探测已连接 MCP 并映射。详见 `Expert.md`。
5. **输出 md 优先**：所有文书技能默认先产出 Markdown 交付物，经 `/legal-verification` 放行后再询问"是否转 Word"。

---

## 连接器一览

### 法律检索后端（元典 / 北大法宝，可单用·可灾备）

| 项目 | 说明 |
|------|------|
| **用途** | 法条精确检索、语义检索、法规详情查询、案例检索、现行有效性验证、引用一键核验 |
| **供应商** | 华宇元典 / 北大法宝 / 其他同类法律数据库 MCP；可单选或都配，都配时按首选→灾备 |
| **使用技能** | 法律核验 / 要件攻防分析 / 主诉诉状 / 被诉答辩状 / 质证意见 / 庭审提纲 / 代理词 / 程序性文书系列 / 证据目录 / 法律研究报告 / 新法解读 |
| **核心价值** | 确保引用法条现行有效，消除 [L4-法条待验证] 标记 |
| **降级方案** | 法条标 [L4-法条待验证]，建议用户通过裁判文书网/官方法规库人工检索 |
| **配置方式** | 在你的 Agent 平台的连接器/MCP 设置中搜索"元典"或"北大法宝" |

### 工商信息后端（企查查 / 同类）

| 项目 | 说明 |
|------|------|
| **用途** | 公司工商信息、股东结构、对外投资、关联关系、司法案件、失信被执行人、股权冻结 |
| **供应商** | 企查查 / 天眼查 / 启信宝 / 其他工商信息 MCP |
| **使用技能** | 财产线索调查 / 主诉诉状 / 程序性文书系列 / 新法解读 |
| **降级方案** | WebSearch 降级，标 [L4-主体信息待验证] |

### 全网搜索（SEARCH.* 能力槽）

| 项目 | 说明 |
|------|------|
| **用途** | 补充法律研究、公开信息检索、政策法规更新确认 |
| **供应商** | 任意已连接的搜索类 MCP（GoogleWebSearch / IflowWebSearch / Tavily 等） |
| **降级方案** | 依赖模型内置知识，标注 [L4-信息待验证] |

### 文档解析后端（输入侧，多后端可选 + 灾备）

| 项目 | 说明 |
|------|------|
| **用途** | 将上传文件解析为结构化 Markdown（含表格、公式、标题层级） |
| **可选后端** | MinerU / 阿里云百炼 / markdownify / 自部署 marker/docling 等 |
| **降级链** | 首选后端 → 其他已连接后端 → 内置 pypdf → fitz 视觉降级 → 内置 Read |

### Markdown→Word 转换后端（输出侧，可选）

| 项目 | 说明 |
|------|------|
| **用途** | 将 Markdown 文书渲染为符合中文法院提交习惯的 .docx Word 文件 |
| **可选后端** | 任何能提供 `DOCX.md_to_docx` 能力的工具/CLI/MCP |
| **降级方案** | 仅交付 Markdown 并提示用户在本机手动转换 |

### 协作平台后端（COLLAB.* 能力槽）

| 项目 | 说明 |
|------|------|
| **用途** | 案件管家台账、知识库团队协作、通讯录、日历、邮箱 |
| **供应商** | 钉钉（dws）/ 飞书（lark-cli）/ 其他 OA 平台 |
| **降级方案** | 本地 JSON + Markdown 零依赖运行 |

### 知识库后端（KB.* 能力槽，六种后端可选）

| 项目 | 说明 |
|------|------|
| **用途** | 诉讼知识库三区（部门法知识库/办案笔记/工作笔记）的语义检索与RAG问答 |
| **支持后端** | qmind / 飞书知识库 / 钉钉知识库 / Obsidian Vault + Grep / ima 知识库 / 语雀 |
| **混用方案** | 可按三区分开绑定（如 A区=ima、B区=Obsidian、C区=飞书） |
| **降级方案** | 本地 Read/Grep；均不可用则仅依赖对话内上下文 |

---

## 连接器组合建议

### 最小配置（个人使用）

无需任何连接器。所有技能以本地模式运行，法条标注 [L4-法条待验证]。

### 推荐配置（日常诉讼工作）

- **法律检索后端**（元典或北大法宝任一）
- **工商信息后端**（企查查或同类）
- **知识库后端**（Obsidian Vault 或 qmind）

### 完整配置（追求最大效率）

全部连接器接入：法律检索（元典 + 北大法宝灾备）+ 工商信息 + 全网搜索 + 文档解析 + 协作平台 + 知识库（qmind + Obsidian 混用）。
