# 诉讼实践画像

*由用户或 AI 协助维护。如需更新，直接修改本文件中的对应字段即可。*

---

## 使用者

| 字段 | 值 |
|------|------|
| 角色 | [PLACEHOLDER — 执业律师/公司法务] |
| 姓名 | [PLACEHOLDER] |
| 团队/组织 | [PLACEHOLDER] |
| 主要业务领域 | [PLACEHOLDER] |
| 诉讼立场 | [PLACEHOLDER — 主诉为主/被诉为主/混合] |

---

## 风险与风格

| 字段 | 值 |
|------|------|
| 风险偏好 | [PLACEHOLDER — 一句话描述] |
| 文书语调 | [PLACEHOLDER — 正式/半正式/因场景而异] |
| 代理词风格 | [PLACEHOLDER — 攻击型/稳守反击型/说理型/综合型] |

---

## 案件管理

> 律师/法务双模式根据「角色」自动切换：律师启用利益冲突门禁，法务启用外部律师管理。

| 字段 | 值 |
|------|------|
| 协作平台 | [PLACEHOLDER — 钉钉 / 飞书 / 暂无] |
| 台账 tableId | [PLACEHOLDER] |
| 提醒渠道 | [PLACEHOLDER — IM推送 / 日历日程 / 仅查看] |
| 定时提醒时间 | [PLACEHOLDER — 默认 09:00] |

### 办案流程

```
[PLACEHOLDER — 用户自定义流程]

默认通用流程：收案建档 → 事实梳理 → 检索研究 → 文书起草 → 证据整理 → 庭审准备 → 开庭 → 庭后代理词 → 等判决 → 结案复盘
```

---

## 知识库方案

> 三区架构：A区（部门法知识库·公域检索）、B区（办案笔记·私域沉淀）、C区（工作笔记·私域沉淀）。
> 支持六种后端，可按三区分开绑定实现混用。

| 字段 | 值 |
|------|------|
| A区后端 | [PLACEHOLDER — qmind / 飞书 / 钉钉 / Obsidian / ima / 语雀] |
| B区后端 | [PLACEHOLDER — qmind / 飞书 / 钉钉 / Obsidian / ima / 语雀] |
| C区后端 | [PLACEHOLDER — qmind / 飞书 / 钉钉 / Obsidian / ima / 语雀] |

### 知识库注册表

| 域名 | 标题 | 后端 ID / 路径 | 状态 |
|------|------|---------------|------|
| civil-code | 民法典 | [PLACEHOLDER] | 待配置 |
| civil-procedure | 民事诉讼法 | [PLACEHOLDER] | 待配置 |
| company-law | 公司法 | [PLACEHOLDER] | 待配置 |
| contract-law | 合同法 | [PLACEHOLDER] | 待配置 |
| case-notes (B区) | 办案笔记 | [PLACEHOLDER] | 待配置 |
| work-notes (C区) | 工作笔记 | [PLACEHOLDER] | 待配置 |

### 沉淀路由

| 产出来源 | 沉淀到 |
|---------|--------|
| /case-review | B区 办案笔记 |
| /element-analysis | B区 办案笔记 |
| /legal-research、/new-law-explained | C区 工作笔记 |
| 用户主动上传法律资料 | A区 对应域名 |

---

## 外部能力后端

> 运行时自动探测已连接的 MCP 并使用。同类配多家则自动灾备。全不可用走降级。

| 能力类别 | 首选 | 降级方案 |
|----------|------|----------|
| 法律检索 | [PLACEHOLDER] | 标注 [L4-法条待验证]；在你的 Agent 平台连接器/MCP 设置中搜索"元典"或"北大法宝" |
| 工商信息 | [PLACEHOLDER] | WebSearch 降级，标注 [L4-主体信息待验证]；搜索"企查查" |
| 文档解析 | [PLACEHOLDER] | 内置 pypdf/fitz/Read |
| Word 转换 | [PLACEHOLDER] | 仅交付 md |
| 全网搜索 | [PLACEHOLDER] | 依赖模型内置知识并标注 [L4-信息待验证] |
| 协作平台 | [PLACEHOLDER — 钉钉/飞书] | 本地 JSON+Markdown，标注 [本地模式-协作平台未连接] |
| 知识库 | [PLACEHOLDER] | 仅依赖对话内上下文，不持久化，标注 [知识库不可用] |

---

## 案件级配置（Matter Profile）

全局画像定义"我是谁"，案件画像定义"这个案件用什么策略"。

- **路径**：`matters/{slug}/matter.md`（由案件管家建档时自动生成）
- **激活**：说"切换到 X 案"
- **覆盖规则**：matter.md 有值字段覆盖 profile.md 同名字段；优先级 matter.md > profile.md > 默认值

---

## 输出规范

### md 优先

所有文书默认先产出 .md → 经 `/legal-verification` 放行 → 询问是否转 Word。

### 溯源标签（四级）

| 级别 | 含义 |
|------|------|
| L1 | 经法律检索/工商后端核验 |
| L2 | 用户上传原件/法院文书 |
| L3 | 用户陈述 |
| L4 | 模型推断-待验证 |

### 标记词汇

`[待验证:事实]` / `[需引用:法条]` / `[请审阅:主观判断]` / `[证据缺口]` / `[风险提示]`

---

## 共享护栏

以下规则适用于本套件所有技能，不可覆盖：

1. **无沉默补充** — 缺信息标标记，不编造
2. **法条核实闸门** — 正式文件输出前必过 `/legal-verification`，全部准确方可放行
3. **文档解析前置** — 收到上传文件后先解析为 Markdown 再进入分析，绝不臆造文档内容
4. **请求权基础思维** — 文书类技能以请求权基础方法为分析起点
5. **案件数据真实性** — 绝不臆造 ID/记录，缺失时向用户索取
6. **保密与隔离** — 不跨案件泄露信息；案件配置互不污染
7. **md 优先输出** — 先 md → 核验 → 放行 → 询问转 Word
8. **案件管家自动同步** — 任何技能产出案件相关成果后自动同步更新案件管家
9. **后端平台无关** — 不硬编码特定 MCP 工具名，按能力槽运行时探测使用

---

## MCP 预检协议

> 各技能不必重复以下协议全文，只需声明自身使用的后端类别（如 `LAW.*`），即自动继承此协议。

### 后端探测

执行任何需要外部能力的技能前，先调用 `MCP.list_tools` 探测当前会话可用的 MCP 服务：

| 后端类别 | 探测关键词 | 映射能力槽 |
|---------|-----------|-----------|
| 法律检索 | `law` / `yuandian` / `pkulaw` / `法宝` | LAW.* |
| 工商信息 | `qcc` / `企查查` / `天眼查` / `biz` | BIZ.* |
| 协作平台 | `dws` / `dingtalk` / `钉钉` / `lark` / `飞书` | COLLAB.* |
| 全网搜索 | `全网搜索` / `web_search` / `search` | SEARCH.* |
| 文档解析 | `parse` / `mineru` / `百炼` / `markdown` / `docling` | PARSE.* |
| Word 转换 | `md_to_docx` / `docx` / `word` | DOCX.md_to_docx |
| 知识库 | `qmind` / `knowledge` / `obsidian` / `ima` / `notebook` | KB.* |

### 探测结果处理

1. **全部可用** → 静默进入工作流。
2. **部分不可用** → 告知用户并询问是否以降级模式继续。
3. **全部不可用** → 同上询问后降级执行。

### 降级规则

| 后端类别 | 降级标注 | 降级行为 |
|---------|---------|---------|
| 法律检索 (LAW.*) | `[L4-法条待验证]` | 保留法条引用但标注，继续生成 |
| 工商信息 (BIZ.*) | `[L4-主体信息待验证]` | 保留用户提供的主体信息但标注 |
| 协作平台 (COLLAB.*) | `[本地模式-协作平台未连接]` | 回退本地 JSON + Markdown |
| 全网搜索 (SEARCH.*) | `[L4-信息待验证]` | 依赖模型内置知识并标注 |
| 文档解析 (PARSE.*) | `[视觉降级-待人工复核]` | 按降级链回退 |
| Word 转换 (DOCX.md_to_docx) | 无标注 | 仅交付 Markdown |
| 知识库 (KB.*) | `[知识库不可用]` | 仅依赖对话内上下文 |

**核心原则**：后端不可用 **不阻塞** 文书生成，仅在对应位置加注标签。

---

## 法律核验闸门

> 各技能不必重复闸门全文，只需写一行 `> ⛔ 交付前必过闸门（见 Expert.md）`，并注明核验对象。

交付正式文件之前，调用 `/legal-verification` 对文书全文的法规、法条、案例引用逐条核验：

- **放行标准**：判定全为「准确 / 准确(有省略)」
- **失败处理**：出现「时效风险 / 语义不一致 / 未命中」→ 修正后重新核验
- **降级处理**：法律检索后端不可用时按 MCP 预检协议降级标注 `[L4-法条待验证]` 并提示人工复核
- **底线**：不得跳过闸门

---

## 文书 Word 排版规范

### 输出流程（md 优先）

核验放行后先产出 md 成品，随后询问用户「是否转 Word」，确认才转 .docx。

**文件命名**：`{案件简称}_{文书类型}_{日期}.md`，Word 版同名 `.docx`。

### 排版参数源（format-spec.md）

Word 转换与校验的所有参数——字体、字号、行距、页边距、屏蔽名单、引用格式白名单、校验阈值——统一外部化到套件根目录的 `format-spec.md`（单一权威源）。`scripts/md2docx_legal.py` 与 `scripts/verify_docx.py` 均以此为参数源。

- 编辑 `format-spec.md` 修改默认参数
- 复制为 `format-spec.<court>.md` 按法域/法院分派多份规范
- 不指定 `--spec` 时脚本自动发现套件根目录的 `format-spec.md`

**转换命令**：
```bash
python3 scripts/md2docx_legal.py --spec format-spec.md input.md output.docx
python3 scripts/verify_docx.py --spec format-spec.md output.docx
```

### 核心参数速查（详见 format-spec.md）

以下为 `format-spec.md` 默认值的速查表，**实际参数以 `format-spec.md` 为准**。如本表与 `format-spec.md` 冲突，以 `format-spec.md` 为权威。

| 项目 | 规格 |
|------|------|
| 纸张 | A4 |
| 标题 | 二号宋体（不加粗），居中 |
| 正文 | 四号宋体，首行缩进 2 字符 |
| 行距 | 固定值 25 磅 |
| 页边距 | 上下 2.54cm，左右 3.17cm |
| 落款 | 右对齐；"此致"首行缩进 2 字符；法院名称顶格左对齐 |
| 页码 | 底部居中 |

> **不再区分 Preset A / B**：正式文书与工作底稿均使用上述统一参数。差异仅在内容规则（如「法律研究报告」/「新法解读」默认交付 Markdown 不转 Word），由各技能自身声明，不通过排版参数区分。

---

## 标准管线

```
用户输入 → 文档解析(有上传文件时) → MCP预检(按需) → 分析/起草(md草稿) → /legal-verification(闸门) → md交付 → 询问转Word → 同步案件管家
```

---

## 配置版本

- 模板版本: v2.0.0
- 适用套件: cn-litigation-toolkit（通用发布版）
- 最后更新: [由用户维护]
