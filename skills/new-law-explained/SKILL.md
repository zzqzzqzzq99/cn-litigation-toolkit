---
name: new-law-explained
version: "1.0.0"
description: >-
  对刚发布/修订的中国法律法规进行律所级深度解读（Markdown 格式）。当用户提及
  "新法解读""新规速评""新规解读""法律修订""某某法出台/修订/施行""帮我解读《XX法》"
  或粘贴新法/修订稿文号时触发。以新旧对比表为基石，经法律检索后端（元典/北大法宝，运行时自动适配+灾备，见 profile.md「外部能力后端」）检索校验新旧法文本，
  按"精准锚定→深度解构→动态校验"三步走，自动读取用户画像（行业/业务形态/客户领域）
  与执业身份切换视角，并主动询问应用目标（公众号/内部分享/客户培训等）、解读深度
  （速览/标准/深度）、是否结合特定公司情况落地，复用头部律所实务映射输出。
---

# 新法解读（new-law-explained）

把刚发布或修订的法律法规，从"法条复读"升级为"实务映射"。本技能对标头部律所数百篇新法解读的共性手法——**以新旧对比表为基石，做制度提炼与风险量化，落到用户所处行业/客户领域的合规动作**，输出一份结构清晰、依据透明的《新法解读》（**仅 Markdown 为核心交付**，按应用目标可选导出公众号 HTML 或 Word）。

与 `/legal-research` 的分工：法律研究报告回答"某个具体法律问题该怎么定性/有何风险"；本技能针对"一部新发布或修订的法规整体"做体系化解读，核心差异是 **新旧对比驱动** 与 **行业落地清单**。

---

## 核心设计

### A. 双视角结论导向（读画像自动切换）

| 视角 | 适用画像 | 解读落点 | 关键措辞 |
|------|----------|----------|----------|
| **律师视角** | 外部律师 / 执业律师 | 新规对**所服务客户/执业领域**的影响 + 律师可提供的服务切入点 | "客户/委托人""可协助客户……""业务机会与风险提示" |
| **法务视角** | 公司法务 / 法律顾问 / 合规 | 新规对**本公司业务形态/所处行业**的影响 + 法务的内部合规动作 | "我司/本公司""内部应排查……""合规整改建议" |
| **客观研究** | 身份不明 / 纯学理梳理 | 仅做制度变化的客观解读，不预设委托或业务场景 | 中立陈述 |

### B. 三档解读深度（决定篇幅与工序）

| 档位 | 篇幅参考 | 包含内容 | 是否联网/查企 |
|------|----------|----------|---------------|
| **速览** | ≤1500 字 | 新旧对比表 + 3-5 条核心变化要点 | 否（仅法律检索后端取文本） |
| **标准** | 3000–5000 字 | 速览 + 核心制度深度解析 + 本行业合规清单 | SEARCH.* 取官方解读 |
| **深度** | 8000 字+ | 标准 + 域外对标 + 争议解决预判 + 结合特定公司的落地建议 | SEARCH.* + 工商信息后端（BIZ.*，见 profile.md）（如需落地） |

### C. 应用目标适配（决定语气与导出格式）

| 应用目标 | 语气风格 | 结构侧重 | 默认导出 |
|----------|----------|----------|----------|
| **公众号/对外发布** | 传播友好、标题有钩子、克制法言法语 | 强导读、金句小标题、对比表前置 | md + 可选 HTML（HTML 渲染后端） |
| **内部分享/法务简报** | 务实、要点先行 | 影响矩阵 + 合规清单 | md + 可选 Word（Word 转换后端, DOCX.md_to_docx） |
| **客户/业务部门培训** | 通俗、问答式、可照做 | Checklist 与流程卡为主 | md + 可选 Word |
| **个人研究/底稿** | 严谨完整、保留推演 | 制度解析 + 域外对标 | md |

> 视角来自**画像自动判定**；深度、应用目标、是否结合特定公司**主动询问用户一次**（见第 0 步）。

---

## 前置条件

| 序号 | 条件 | 必要性 | 说明 |
|------|------|--------|------|
| 1 | 新法名称 / 文号 / 全文文本 | 必需 | 用户提供其一即可；仅有名称时由法律检索后端取全文 |
| 2 | 套件根目录 `profile.md` | 推荐 | 提取行业/业务形态/客户领域/执业身份，自动定制视角与行业落点；无则询问一次 |
| 3 | 法律检索后端服务 | 推荐 | 取新旧法文本、校验条号与时效；不可用时降级（见降级策略） |
| 4 | SEARCH.*（搜索 MCP） | 可选 | 取官方解读、答记者问、律所文章；标准/深度档使用 |
| 5 | 工商信息后端 | 可选 | 仅"结合特定公司落地"时核验主体与经营状况 |

---

## 第 0 步：画像捕捉 + 应用参数询问（必做，先于任何检索）

1. **读取画像**：尝试读取套件根目录 `profile.md`，提取以下字段用于定制解读落点：
   - 执业身份（律师 / 法务 / 其他）→ 决定**视角**
   - 法务场景：所在公司、所处行业、业务形态
   - 律师场景：主要服务客户所在行业、执业领域
   - 风险偏好（保守/进取）→ 影响风险分级措辞与提示密度
   - 画像缺失或字段不全 → 在下一步询问中补齐"面向行业"一项。

2. **主动询问用户一次**（用 AskUserQuestion，一个批次问完，不要逐条追问）：
   - **应用目标**：公众号/对外发布 ｜ 内部分享/法务简报 ｜ 客户/业务部门培训 ｜ 个人研究底稿
   - **解读深度**：速览 ｜ 标准 ｜ 深度
   - **是否结合特定公司落地**：否（仅做行业普适解读）｜ 是（提供公司名 → 触发工商信息后端/联网核验经营与风险，给定制化整改动作）
   - **面向行业/客户领域**：默认取画像值；画像无则让用户指定（如金融/TMT/制造/跨境贸易/医疗/房地产/能源等）

3. 记住本次选择，全程不再重复询问。纯学理梳理（用户明确只要客观制度解读、无落地场景）→ 视角强制为「客观研究」，可跳过"特定公司落地"。

---

## 工作原则

1. **对比表是基石**：任何档位都必须先产出新旧对比表，后续解读全部由对比表派生，杜绝脱离条文的"泛泛而谈"。
2. **每引必校验**：正文每引用一条法条/条号/生效日期，立即经法律检索后端核验原文、条号、时效与修订状态；核验不通过用权威原文替换或标注，**绝不用记忆编造法条**。
3. **实务映射而非法条摘要**：制度提炼必须答"改变了什么游戏规则 + 对本行业意味着什么 + 该做什么动作"，而非复述条文。
4. **不假设未给信息**：不替用户脑补事实；结合特定公司时所有结论须基于工商信息后端/联网取得的可核验信息，缺口明确标注。
5. **时效与冲突**：优先"现行有效"文本；标注生效日期、过渡期、新旧衔接；上位法>下位法、新法>旧法、特别法>普通法。
6. **客观中立**：解读用于识别风险与机会，负面影响与不确定性同样如实呈现，不为单方背书。
7. **积分意识**：案例/详情类调用先取摘要，确需全文再调 detail 类工具，调用前向用户说明。
8. **言简意赅，实事求是**；结论加粗，避免晦涩法言法语。

---

## MCP 工具映射

本套件统一走 MCP，**不依赖任何本地脚本或 API Key 配置**。调用前先用 `MCP.describe_tool` 确认参数 schema。

> 实际工具名由运行时 `MCP.list_tools` 探测解析，映射见 profile.md「外部能力后端」；本表只列能力名，不绑定供应商。

#### MCP 预检

正式检索前，先调用 `MCP.list_tools` 探测法律检索后端（关键词：`law`/`yuandian`/`pkulaw`/`法宝`）、SEARCH.*（关键词：`search`/`搜索`/`web`/`AiSearch`）和工商信息后端（关键词：`qcc`/`企查查`）是否可用。
- **法律检索不可用** → 用 `AskUserQuestion` 告知用户："法律检索 MCP 未连接，新法条文校验将降级。建议前往宿主平台的连接器/MCP 设置（Agent 运行时 / Claude Code / Cursor / Gemini CLI / OpenCode 等） 中配置元典或北大法宝。是否暂不配置、以降级模式继续？"
- **SEARCH.* 不可用** → 告知用户："全网搜索 MCP 未连接，官方解读与二手文献阶段将跳过。是否继续？"
- **工商信息后端不可用** → 告知用户："工商信息 MCP 未连接，结合特定公司落地阶段将跳过。是否继续？"

> 探测协议详见 Expert.md「MCP 预检协议」。

### 取新旧法文本与校验（法律检索后端·基石）

| 用途 | 能力名 | 说明 |
|------|----------|------|
| 法规关键词检索 | `LAW.statute_search` | 按名称/文号定位新法与被废止/修订的旧法 |
| 法规详情全文 | `LAW.statute_detail` | 取新法全文与旧法全文，逐条对比的数据底座 |
| 法条关键词检索 | `LAW.article_search` | 已知精确条号/术语时用 |
| 法条详情原文 | `LAW.article_detail` | 校验单条原文、条号与时效 |
| 法条语义检索 | `LAW.semantic_search` | 找配套细则/上位法/相关条文 |
| 案例检索（深度档争议预判） | `LAW.case_search` / `LAW.case_semantic` | 预判新规对举证/管辖/裁判口径的影响 |
| 案例详情 | `LAW.case_detail` | 第二阶段，需裁判说理时调用 |

### 取官方解读与二手文献（标准/深度档）

| 用途 | MCP 工具 | 说明 |
|------|----------|------|
| 官方解读/答记者问/律所文章 | `SEARCH.web_search` / `SEARCH.ai_search` | 先官方解读，再头部律所文章 |
| 链接正文提取 | WebFetch（内置） | 提取检索到文章的正文 |

关注：立法机关/主管部门官方解读、答记者问、配套细则；头部律所文章。

### 结合特定公司落地（仅"是"时启用，工商信息后端）

| 用途 | 能力名 | 说明 |
|------|----------|------|
| 主体核验 | `BIZ.company_detail` | 确认公司存续、经营范围、行业归属 |
| 经营/资质 | `BIZ.company_detail`（资质/许可） | 行政许可、资质，判断新规适用面 |
| 风险信息 | `BIZ.risk` | 行政处罚、合规风险，定制整改优先级 |

---

## 三步走工作流

### 阶段一 · 精准锚定与对比（基石步骤）

> 📎 加载 `references/comparison-risk-industry.md` 作为新旧对比表规范与风险分级矩阵参考。

1. **取新法全文**：用 `LAW.statute_search` 按名称/文号定位 → `LAW.statute_detail` 取正式稿全文。
2. **取对比基准（旧法/草案）**——按答复"法律检索后端优先 + 多源降级"：
   - 优先经法律检索后端检索被修订/废止的旧法或上一版本全文；
   - 法律检索后端取不到 → SEARCH.* 取征求意见稿/官方对照说明/废止令，并标注来源；
   - 仍取不到 → 在对比表中标注 `[需人工核对]`，继续完成可比部分，绝不编造旧法内容。
3. **生成新旧对比表**（Markdown，全文基石）：

   **⚠ 硬性格式要求：逐条引用原文，不得仅写"要点"概括。** 每条有变化的条款用独立小节呈现，结构如下：

   ```markdown
   ### 第X条：{条文主题}

   **新解释第X条**：

   > {引用新法该条完整原文}

   **旧解释第X条**：

   > {引用旧法该条完整原文}

   **变化**：~~改~~ / [+] 新增 / [-] 删除。{核心变化简述，包括实务影响分析}
   ```

   - 变化类型标注：新增 `[+]`；删除 `[-]`；修改用 `~~改~~`。
   - **原文引用规则**：新旧法条文必须引用法律检索后端取回的正式原文，不得缩写、概括或省略关键字句。如单条原文过长（超过 500 字），可省略与变化无关的段落并以"……（省略）"标注，但涉及变化的关键句段必须完整引用。
   - 速览档对比表即为主体；标准/深度档对比表作为后续解析的索引。

### 阶段二 · 深度解构（基于对比表，标准/深度档）

4. **提炼核心制度**：从对比表中挑出 3-5 个真正"改变游戏规则"的制度（如"安全港""分级管理由三级变五级""备案改审批"），逐个说明立法意图、与旧规差异、实务后果。
5. **风险量化分级**：对每项变化打标签——
   - 🔴 **高危**：涉刑事责任 / 高额罚款 / 业务资格丧失
   - 🟡 **中危**：行政备案/许可 / 合同效力 / 整改期限
   - 🔵 **低危**：内部流程 / 文本修订 / 审批权限调整
6. **行业影响矩阵**：针对画像/用户指定的行业，列"业务场景 → 受影响点 → 具体合规动作"（动作要可执行，如"修订委托营销协议第 X 条""新增数据出境申报流程"）。
7. **域外对标**（仅深度档）：如涉数据/反垄断/合规等有国际可比性的领域，简要对比域外制度（如 GDPR、域外 AEO）以衬托立法取向，须标注来源、不编造。

### 阶段三 · 动态校验与落地（输出前）

8. **时效性与状态校验**：经法律检索后端确认新法是否已生效、有无后续修正、过渡期安排；关键数额/期限/条号逐一回校原文。
9. **结合特定公司落地**（仅用户选"是"）：用工商信息后端核验该公司主体、经营范围、资质与现有风险，把行业影响矩阵收敛为该公司的定制整改清单（含优先级）；信息不足处明确标注，不臆测。
10. **交叉验证关键条款**：对罚款数额、生效日期等关键点，官方解读与律所/实务文章口径不一致时如实并列，不强行取一。

---

## 报告结构（Markdown）

> 📎 加载 `references/firm-article-anatomy.md` 作为头部律所文章结构骨架与写作手法参考。

要素**按深度档与应用目标灵活裁剪**：标 `[核心]` 必备，`[标准+]` 标准档起，`[深度]` 仅深度档。

```markdown
# [new-law-explained]《XX法》核心变化与实务指引

> 文号：{令第X号} ｜ 发布/施行：{日期} ｜ 面向：{行业} ｜ 视角：{律师/法务/客观} ｜ 深度：{速览/标准/深度}

## 一、一句话速览  [核心]
{2-3 句讲清"这部新规改了什么、对谁影响最大"。公众号目标可作导语。}

## 二、新旧对比表  [核心]
{阶段一产出的对比表，全文基石。每条用独立小节，引用新旧法原文段落，标注变化类型与核心变化简述。}

## 三、核心制度深度解析  [标准+]
{阶段二提炼的 3-5 项制度，每项含：变化点 / 立法意图 / 与旧规差异 / 实务后果 / 风险等级标签}

## 四、行业影响与合规清单  [标准+]
| 业务场景 | 受影响点 | 风险等级 | 合规动作（Checklist） |
|---------|---------|---------|----------------------|

## 五、结合本公司的落地建议  [按需·选"是"时]
{基于工商信息后端核验的该公司定制整改清单 + 优先级；信息缺口标注}

## 六、域外对标  [深度]
{与域外可比制度的简要对照，标注来源}

## 七、争议解决预判  [深度]
{新规对诉讼举证责任、管辖、裁判口径的潜在影响，引相关案例/条文}

## 八、依据与来源清单  [核心]
**新法/旧法**：{名称+文号+条号+时效}
**官方解读**：{标题 + URL}
**二手参考**：{机构/作者《标题》 + URL}

---
**法律声明**：本报告基于公开法律数据库与公开信息生成，不构成正式法律意见；引用前请核对官方文本。
```

> 速览档：仅保留一、二、八节 + 核心变化要点。
> 公众号目标：可将一节扩为导语、给小标题加钩子，但对比表与依据清单不得省略。

---

## 输出与交付

> **⛔ 交付前必过闸门**（见 Expert.md「共享护栏」）：核验对象为解读全文（含法条生效日期）。

1. **核心交付为 `.md`**：命名 `new-law-explained_{法规简称}_{日期}.md`，保存至用户指定文件夹或当前输出目录，以 `file://` 链接交付。
2. **按应用目标可选导出**（询问或直接按第 0 步选择执行）：
   - 公众号/对外发布 → 调 `HTML 渲染后端` 生成排版友好的 HTML；
   - 内部分享 / 客户培训 → 调 `Word 转换后端`（DOCX.md_to_docx）生成 Word。

> **转 Word（可选）**：本技能产出为内部参考/分析文件，非法院提交正式文书，不强制经过文书排版闸门。如用户选择转 Word，使用 `scripts/md2docx_legal.py` 转换脚本（排版参数详见 `format-spec.md`）。

3. **输出后简报**：说明法律检索后端调用了哪些工具各几次、检索到的新旧法版本、SEARCH.* 关键词、工商信息后端是否启用及消耗提示。
4. **md 优先**：md 完成并经 `/legal-verification` 放行后，询问用户是否进一步转 Word（Word 转换后端（DOCX.md_to_docx））。

---

## MCP 降级策略

| MCP 不可用 | 降级方案 |
|-----------|----------|
| 法律检索后端取新法全文 | 提示用户粘贴新法正式稿文本，基于用户文本解读并标注"未经权威库校验" |
| 法律检索后端取旧法/对比基准 | SEARCH.* 取对照说明/征求意见稿；仍无则对比表标 `[需人工核对]`，绝不编造旧法 |
| 法律检索后端法条校验 | 关键条号标"[L4-法条待验证]"，附待核对清单，绝不用记忆替代 |
| SEARCH.* | 跳过官方解读/二手阶段，仅基于法条文本解读，报告中声明未做二手调研 |
| 工商信息后端 | 跳过"结合本公司落地"，退回行业普适合规清单，并告知用户 |
| HTML 渲染后端 / Word 转换后端（DOCX.md_to_docx） | 仅交付 .md，提示用户可后续自行转换 |
| 无 `profile.md` | 在第 0 步询问中补齐视角与面向行业 |

---

## 硬性约束

- **禁止编造法条/旧法/数额/生效日期**：一切以法律检索后端原文或用户提供的正式文本为准，取不到如实标注。
- **对比表不得省略**：任何档位都必须先有新旧对比表。
- **对比表必须引用原文**：每条对比必须引用新旧法的完整原文段落，不得仅以"要点"或概括性描述代替；原文过长可省略无关段落（标"……（省略）"），但涉及变化的关键句段必须完整引用。
- **不假设未给信息**：结合公司落地的结论须基于可核验信息，缺口明确标注。
- **仅 .md 为核心交付**；HTML/Word 仅按应用目标可选导出。
- 报告末尾必须附**法律声明**免责条款。
- 第 0 步未完成画像捕捉与参数询问前，不开始检索。

---

## 质量检查清单

### 视角与参数
- [ ] 已读取/询问确定视角（律师/法务/客观）、深度（速览/标准/深度）、应用目标、面向行业
- [ ] 解读落点贴合用户行业/客户领域，而非泛泛而谈
- [ ] 篇幅与所选深度档匹配

### 对比与解构
- [ ] 新旧对比表已生成，每条引用新旧法原文段落，变化类型标注规范（[+]/[-]/~~改~~）
- [ ] 核心制度 3-5 项（标准+档），每项有风险等级标签
- [ ] 行业影响矩阵的合规动作可执行（标准+档）

### 检索与校验
- [ ] 新法/旧法全文均经法律检索后端取得（或如实标注降级来源）
- [ ] 关键条号/数额/生效日期已回校原文
- [ ] SEARCH.* 引用均附来源 URL（标准+档）
- [ ] 结合公司落地的信息均经工商信息后端核验（选"是"时）

### 交付
- [ ] 必备节齐全，按档位/应用目标裁剪合理
- [ ] 依据与来源清单完整（新旧法/官方解读/二手分列）
- [ ] 法律声明免责条款已附
- [ ] 仅 .md 为核心交付，已以 file:// 链接交付；按需导出 HTML/Word

---

## 本技能不做什么

- **不替代正式法律意见**：输出是解读底稿，最终口径由律师/法务负责。
- **不编造旧法或对比基准**：取不到对比文本时如实标注，不臆造"旧规如何如何"。
- **不脱离条文空谈**：所有解读必须能回指到对比表的具体条款。
- **不替用户脑补公司情况**：未经核验的公司信息不写入落地清单。
- **不做个案胜诉预测**：争议解决预判仅为方向性提示，个案分析交 `/element-analysis`、`/legal-research`。

---

## 知识库调用（按 profile.md 知识库注册表）

> 新法解读过程中检索已有文献作为参照基准，解读完成后推荐沉淀。

| 环节 | 目标知识库 | 调用方式 |
|------|-----------|---------|
| 新旧对比参照 | 对应知识库 | `KB.retrieve(query="<旧法条文号>")` |
| 学理解释参考 | civil-code / contract-law 等 | 按涉及的法规领域选择对应 notebook |
| 完成后沉淀 | C区 work-notes | `KB.upload(domain=work-notes, file=<解读.md>)` |

降级：CLI 不可达 → 本地 Read/Grep，标注 `[KB:降级-本地]`。

## 错误处理

- 信息不足（无法名/文号/文本） → 补问，不开始检索
- 第 0 步参数未确定 → 先问应用目标/深度/落地，再推进
- 法律检索后端返回空 → 如实告知，提示用户粘贴正式文本，不编造
- 旧法取不到 → 对比表标 `[需人工核对]`，继续可比部分
- SEARCH.* 超时/不可用 → 降级为仅法条解读并声明
- 行业不明 → 第 0 步询问中要求用户指定面向行业
